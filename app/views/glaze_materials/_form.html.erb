<%= form_with(model: glaze_material, local: true) do |form| %>
  <% if glaze_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(glaze_material.errors.count, "error") %> prohibited this glaze_material from being saved:</h2>

      <ul>
        <% glaze_material.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
